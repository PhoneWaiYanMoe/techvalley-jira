# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Base URL used for OAuth/reset-password redirects (Vercel deployment URL in prod)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# AI provider — not yet chosen (Claude/GPT/Gemini), see CLAUDE.md "Known limitations"
# AI_PROVIDER=
# AI_API_KEY=

# Transactional email for team invites (FR-013) — provider not yet chosen
# (password reset email FR-003 can use Supabase Auth's built-in template instead)
# EMAIL_PROVIDER_API_KEY=
